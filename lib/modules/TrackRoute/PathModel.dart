/*
class PathModel {
  final int from;
  final int to;
  final String geometry;

  PathModel({
    required this.from,
    required this.to,
    required this.geometry,
  });

  factory PathModel.fromJson(Map<String, dynamic> json) {
    return PathModel(
      from: json['from_sequence'],
      to: json['to_sequence'],
      geometry: json['geometry'],
    );
  }
}*/
